fn fact(n: int) -> num {
	if n != 1 return n * fact(n - 1)
	// elseif n > 0 return 1
	else return 0
}

let x = fn() {
	if true {
		return 1
	} else {
		return 1.5
	}
}

let x, y, z = 1, "str", true

print(x, y)

return fact(5)
