fn fact(n: int) -> int {
	assert(n >= 0)
	if n != 0 return n * fact(n - 1)
	else {{return 1}}
}

let add = fn(a: int, b: int) {return a + b}

let iter = {2; 3 4, 5}

for i in iter {
	print(fact(i))
}

let i = 4
while true {
	print(i, fact(i))
	i = i - 1
	if i <= 0 break
}

